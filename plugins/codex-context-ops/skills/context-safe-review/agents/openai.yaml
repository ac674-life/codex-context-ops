interface:
  display_name: Context-Safe Review
  short_description: Review changes for correctness, regressions, security, and test gaps.
  default_prompt: Use context-safe review for this change.

policy:
  allow_implicit_invocation: false
